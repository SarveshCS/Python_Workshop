# Input base and height of the right angle triangle
base = float(input("Enter the base of the right angle triangle: "))
height = float(input("Enter the height of the right angle triangle: "))

# Calculate area
area = 0.5 * base * height

print(f"The area of the right angle triangle is {area} square units")